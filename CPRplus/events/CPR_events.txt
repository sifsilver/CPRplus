# -*- ck2.events -*-

namespace = cpr

character_event = {
	id = cpr.1
	
	is_triggered_only = yes
	hide_window = yes

	trigger = {
		trait = cardinal
		FROM = {
			OR = {
				title = k_papal_state
				title = d_fraticelli
			}
		}
	}
	
	immediate = {
		remove_trait = cardinal
	}
}